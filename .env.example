# Since .env.local is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.
DATABASE_URL=postgresql://postgres:postgres@localhost/unprice
DATABASE_READ1_URL=postgresql://postgres:postgres@localhost/unprice
DATABASE_READ2_URL=postgresql://postgres:postgres@localhost/unprice
DRIZZLE_LOG=false
TRPC_LOGGER=false
UPSTASH_REDIS_REST_URL=UPSTASH_REDIS_REST_URL
UPSTASH_REDIS_REST_TOKEN=UPSTASH_REDIS_REST_TOKEN

# Stripe
STRIPE_API_KEY=sk_test_
STRIPE_WEBHOOK_SECRET=whsec_

# Custom Domains
PROJECT_ID_VERCEL=PROJECT_ID_VERCEL
TEAM_ID_VERCEL=TEAM_ID_VERCEL
VERCEL_TOKEN=VERCEL_TOKEN

# tinybird
TINYBIRD_TOKEN=TINYBIRD_TOKEN
TINYBIRD_URL=https://api.tinybird.co

AUTH_GITHUB_CLIENT_ID=AUTH_GITHUB_CLIENT_ID
AUTH_GITHUB_CLIENT_SECRET=AUTH_GITHUB_CLIENT_SECRET
# Linux: `openssl rand -hex 32` or go to https://generate-secret.vercel.app/32
AUTH_SECRET=AUTH_SECRET
RESEND_API_KEY=RESEND_API_KEY
BASELIME_APIKEY=BASELIME_APIKEY
# openssl rand -base64 32
CACHE_ENCRYPTION_KEY=CACHE_ENCRYPTION_KEY
AUTH_REDIRECT_PROXY_URL=http://localhost:3100/auth
TRIGGER_SECRET_KEY=TRIGGER_SECRET_KEY
NODE_ENV=development
EMIT_METRICS_LOGS=false
ENCRYPTION_KEY=ENCRYPTION_KEY
