drop function if exists "public"."config_org"(user_id uuid, org_id uuid, slug text, type organization_type, name text, image text, description text, role_user organization_roles, tier text, is_default boolean);


