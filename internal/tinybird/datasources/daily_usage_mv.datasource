# Data Source created from Pipe 'get_features_usage_daily_v1'

SCHEMA >
    `customerId` String,
    `featureSlug` String,
    `projectId` String,
    `date` Date,
    `usage_sum` Int64,
    `usage_max` Int16,
    `usage_count` UInt64,
    `usage_last_day` Int16

ENGINE "AggregatingMergeTree"
ENGINE_PARTITION_KEY "toYYYYMM(date)"
ENGINE_SORTING_KEY "customerId, featureSlug, projectId, date"
